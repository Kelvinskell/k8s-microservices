apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ingress-demo
spec:
  ingressClassName: aws-ingress
  defaultBackend:
    service:
      name: hello-world
      port:
        number: 80
